# Daily Report — 2026-09-14

## Summary

New `/api/v1/spectral` endpoint for optical index computation.

## Changes

**`e3ca391` feat: add /api/v1/spectral endpoint for optical index computation**

- **`services/ndvi/src/spectral.rs`** (new, 303 lines): Optical spectral compute pipeline with:
  - Formulas: NDVI `(B08-B04)/(B08+B04)`, NDWI `(B03-B08)/(B03+B08)`, NDMI `(B08-B11)/(B08+B11)`, NDRE `(B08-B05)/(B08+B05)`, EVI `2.5*(B08-B04)/(B08+6*B04-7.5*B02+1)`, IRON_OXIDE `B04/B02`, BIOMASS `68.67*NDVI-10.3`
  - Cloud masking via SCL band (non-zero = cloudy)
  - Nodata handling (any band <= threshold masked)
  - Parallel stats via rayon
  - 9 unit tests
- **`services/ndvi/src/models.rs`**: `SpectralRequest` (bands HashMap, width, height, index_type, cloud_mask, cloud_threshold, nodata) and `SpectralResponse` (mean, min, max, sample_count, cloud_fraction, valid_pixel_fraction, processing_ms).
- **`services/ndvi/src/routes.rs`**: Registered `/api/v1/spectral` POST route. Offloads to `tokio::task::spawn_blocking`.
- **`services/ndvi/src/lib.rs`**: Added `spectral` module.

## Impact

Django `RustSpectralEngine` can now offload optical index computation to Rust. Supports 7 indices with cloud masking and nodata handling.
