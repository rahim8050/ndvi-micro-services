# Daily Report — 2026-06-18

## Summary

Fixed NDVI container boot failure caused by port 8081 conflict with Farm-Intelligence-Platform Celery metrics endpoint. Made host port configurable via `NDVI_PORT` env var (default 8082). Updated Django `.env` to point `NDVI_SERVICE_URL` at the new port.

## Changes

- `docker-compose.yml`: Changed host port mapping from hardcoded `8081:8081` to `${NDVI_PORT:-8082}:8081`
- `.env`: Set `NDVI_PORT=8082` (gitignored)
- Farm-Intelligence-Platform `.env`: Updated `NDVI_SERVICE_URL=http://127.0.0.1:8082` (gitignored)

## Commit

`21e7534` — fix: make host port configurable via NDVI_PORT env var, default 8082
