# Daily Report — 2026-09-09

## Summary

Optimized S1_SMI preprocessing concurrency and offloaded CPU-bound tasks.

## Changes

**`11a7135` perf(sentinel1): optimize S1_SMI preprocessing concurrency and offload CPU tasks**

- **`services/ndvi/src/routes.rs`**:
  - Made Tower concurrency limit configurable via `PREPROCESS_CONCURRENCY_LIMIT` env var (default 32, was hardcoded to 2).
  - Parallelized VV/VH COG tile fetches using `tokio::join!` (was sequential).
  - Offloaded CPU-bound Rayon filtering to `tokio::task::spawn_blocking` to avoid blocking the async runtime.
- **`services/ndvi/src/cog_reader.rs`**: Updated error type to `Box<dyn Error + Send + Sync>` for thread safety.

## Impact

Preprocessing throughput increased with parallel VV/VH fetch + non-blocking CPU filtering. Concurrency limit now configurable without code changes.
