[package]
name = "ndvi-service"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
ndvi-service-core = { path = "services/ndvi" }

[workspace]
members = ["crates/common", "services/ndvi", "services/weather"]
